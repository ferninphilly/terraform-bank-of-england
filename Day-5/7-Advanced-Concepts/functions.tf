# TODO: Exercise 2 - Advanced Functions
# Create locals demonstrating:
# - Collection functions (flatten, setproduct, zipmap)
# - String functions (regex, regexall, replace)
# - Type conversion functions
# - Complex expressions

# Example:
# locals {
#   # Flatten nested lists
#   flat_list = flatten([["a", "b"], ["c", "d"]])
#
#   # Create map from two lists
#   mapped = zipmap(["key1", "key2"], ["value1", "value2"])
#
#   # Regex extraction
#   extracted = regex("([0-9]+)", "version-123")
# }

